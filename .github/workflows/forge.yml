name: forge
on:
  push:
    paths:
      - "forge/**"
      - "forge_bundle.js"
      - "forge_loader_user.js"
      - ".github/scripts/pin_release.py"
      - ".github/workflows/release_pin.yml"
      - ".github/workflows/forge.yml"
  pull_request:
    paths:
      - "forge/**"
      - "forge_bundle.js"
      - "forge_loader_user.js"
      - ".github/scripts/pin_release.py"
      - ".github/workflows/release_pin.yml"
      - ".github/workflows/forge.yml"

permissions:
  contents: write

concurrency:
  group: forge-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: forge
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: forge/package-lock.json
      - run: npm ci
      - run: npm test
      - name: Refresh stable fixtures
        run: |
          npm run fixtures
          python3 ../.github/scripts/commit_generated.py "forge: refresh stable envelope fixtures [auto]" "test/fixtures/envelope/*"
      - name: Verify checked bundle
        run: |
          npm run build
          git diff --exit-code -- ../forge_bundle.js
