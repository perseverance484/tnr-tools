# H. Risk register

**Status:** planning. Each risk names the invariant at stake, the evidence it rests on, how it would surface in Forge Next, and the mitigation the roadmap (section G) carries. Likelihood/impact are the planning owner's estimates and are marked as such; they are not measurements.

Legend: L/I = likelihood / impact (low, med, high). "Gate" names the roadmap acceptance gate that retires or bounds the risk. R-20 onward were added after the consolidated ChatGPT state at `chatgpt/forge-quest-studio-foundation@824c4d58` was read; their L/I cells carry "(est.)" to mark the planning-owner estimate explicitly.

**Cited owners (defined once).** Each abbreviation below is `docs/design/<file>` at `chatgpt/forge-quest-studio-foundation@824c4d58`, cited as `ABBR:line`; the meaning of every rule lives in the owner, this register records only the construction and the fact. SSC = FORGE_NEXT_SAFETY_STATE_PRESENTATION_CONTRACT.md. MOB = FORGE_NEXT_MOBILE_ACCESSIBILITY_AUDIT.md. WCM = FORGE_NEXT_WORKFLOW_COMPONENT_MAP.md. WFA = FORGE_NEXT_WIREFRAME_ANATOMY.md. SCR = FORGE_NEXT_SCREEN_CONTENT_REQUIREMENTS.md. IRM = FORGE_NEXT_INTERACTION_RISK_MATRIX.md. CPY = FORGE_NEXT_UI_COPY_AND_STATE_LANGUAGE.md. ICN = FORGE_NEXT_ICON_MOTION_SEMANTICS.md. SCN = FORGE_NEXT_UX_SCENARIO_MATRIX.md. COL = FORGE_NEXT_COLOR_SEMANTICS.md. AUD = FORGE_CURRENT_UI_UX_BASELINE_AUDIT.md. MS = FORGE_NEXT_MISSION_STUDIO.md. RB = FORGE_NEXT_REPO_BACKED_STUDIO_ARCHITECTURE.md. SBI = FORGE_NEXT_STYLE_BOARD_INVENTORY.md. EXP = FORGE_NEXT_CONTENT_STUDIO_EXPANSION.md. HND = FORGE_NEXT_UI_CONTEXT_HANDOFF.md. VD = FORGE_NEXT_VISUAL_DIRECTION.md, the director-approved visual north star, present in the same export. DS = FORGE_NEXT_DESIGN_SYSTEM_V0_1.md, a Tier B v0.1 proposal. FBRF = `state/prompt_forge_quest_studio_foundation.md` at the same SHA. Files that exist only on that branch carry the prefix `824c4d58:`; unprefixed `forge/src/*`, `forge/test/*`, `.github/workflows/*` and `skills/*` paths are `main@305a28f`. K-32 and higher are proposed ids from this pass's decision register (section K); they are open, not ruled.

| ID | Risk | L/I | Evidence today | How it would bite Forge Next | Mitigation / gate |
|---|---|---|---|---|---|
| R-01 | **Mutation ambiguity and idempotence.** A redesign that simplifies the run surface hides or re-enables re-sends of `SENT` items, or a new "retry" affordance double-creates | med/high | The journal makes `SENT → PLANNED` impossible (`forge/src/storage/journal.mjs:39-47`); `run()` refuses jobs with `SENT` items (`runner.mjs:193`); reconciliation is the only path (`resume()` at `runner.mjs:242-260` calling `resolveSent()` at `reconciler.mjs:94-156`). A friendlier UI is exactly where a "Try again" button appears | Duplicate live rows, placeholder orphans, ambiguous state presented as success | Every new mutation-capable screen is driven by the existing runner API; UI-contract tests assert no control can call a send path while an item is `SENT`; adversarial regressions R1-R5, F4c kept green (gate in every phase, section I). Owner contracts for that test: SSC §3 `SENT` (SSC:121-132), IRM §10 recovery anti-patterns (IRM:321-329), scenario S-18 (SCN:187-195) |
| R-02 | **Auth, session and takeover.** The carrier overlay depends on the game's root layout mounting `ClerkProvider`, on `window.Clerk` exposing `loaded`/`session`, on client-side redirects staying inside the Next root, and on the tab marker surviving navigation | med/high | Source-verified at `bdec2883` and `345d18ac` (`docs/handoffs/FORGE_PROTECTED_AUTH_PIN_RECONCILIATION.md`); the drift check to head `36c5873b` is in `evidence/drift.json`; 0.3.0 failed for exactly this class (`harvests/inbox/tnr_results_1789066888093.json`) | A game deploy that changes the landing route, providers or Clerk runtime silently breaks protected work; a bigger UI increases the surface that assumes the overlay survives client routing | Keep `boot()`'s three-outcome contract and the route-agnostic marker; add a source-drift gate on the host surfaces (`pin_relevance.mjs` already declares them); auth state stays a single object every screen renders from; the browser smoke after each release stays user-owned and read-only first |
| R-03 | **Rate limits.** New screens that poll (a review queue, live diffs, dashboards) spend `publicProcedure` budget the operator cannot see; a 429 costs the player 1% of money and bank | high/med | `Ratelimit.slidingWindow(60, "60 s")` per path per user, trip penalty (`docs/PLAN_2026-09-03_builder_app.md` §6); Forge's budget mirrors it at margin 0.5 (`bucket.mjs`); the carrier page's own traffic is invisible to the budget (`docs/BUILDER_APP_NOTES.md`, "Known debt") | An admin queue that refreshes lists on every tab switch trips the limiter; two tabs (operator and admin) share one user's window | All reads in new surfaces go through `CachedReader`; queue views are cache-first with explicit "refresh" taps and visible budget; no background polling; multi-tab lease extended to reads that spend budget (section F); a per-screen budget test (section I) |
| R-04 | **Capture privacy and public-repo persistence.** Widening captures to reach Builder parity (any `proc`, `select`, full bodies) puts protected, user or session data into a public repository | med/high | Full persistence is allowlisted to seven content point reads with a 512 KiB ceiling (`captures.mjs:58-84`); Builder writes any body (`builder_bundle.js:372-375`); real research manifests read `combat.*` battle data with player names (`push/05`, `push/06`) | A research capture of `profile.*` or `combat.*` bodies committed to `harvests/inbox/` | Data-classification tiers (section F, from the design panel): repo-safe, local-only, projected; every registry entry carries a tier; export refuses anything above its tier; redaction is a manifest-declared projection, never a heuristic; user decision K-06 |
| R-05 | **Stale source pins and contracts.** Two different staleness problems are easy to conflate: Forge's own derived contracts (`fields.json`, `nested.json`, the 43-row registry) come from `345d18ac`, while the skill's generated `45c/45d/45e/45g` come from the older `bdec2883` and `docs/DRIFT.md` already marks 45d BREAKING (12 now-required item farm fields, quest `requiredFarmingLevel`) | med/high | Measured in this pass (`evidence/drift.json`, `deepdive/game-drift-pin-vs-head`): from `345d18ac` to head `36c5873b` (81 commits) the six top-level field sets and nine nested key families regenerate byte-identically, all six validator files are unchanged, and auth class, kind and limiter are identical for all 43 registry paths and all 167 content-router procedures; `pin_relevance.mjs` exits 1 only on import reshuffles, the `idSchema` alias, dead-code removal and additive routers/tables/constants. The sentinel head `98d0eca5` is exactly current with head on every Forge surface. So Forge is not blocked by drift today; the repository's manifest validator is, because it reads the stale 45x set. Two coverage gaps sit beside the staleness: the Event subtype is `needs_compiler` in the Studio registry (`824c4d58:skills/building-tnr-content/data/49_DATA_quest_studio_subtypes.json:28-30`), so "Event next" is new tooling rather than adapter work (K-27); and guide content has zero entries in `45c_DATA_constructors.json`, `45f_DATA_procedures.json` and `forge/src/transport/procedures.mjs` (grep count 0 in this pass, Observed), so a Guide lane has no contract to project (K-29) | A future game deploy that changes a validator or a limiter silently invalidates the bundle; and today a manifest can pass Forge's pinned key sets while `validate.py` refuses or mis-shapes the same farm fields; an Event or Guide phase planned as "another adapter" has no contract underneath it | Keep the derive tools and `pin_relevance.mjs` as a phase-0 gate that runs on the game head of the day; treat moving the Forge pin as a provenance and line-cite refresh (low risk, K-13) and treat the skill's 45x re-extraction through `schema_diff` as the separate, higher-value pass it already is; bundle banner and Settings carry the pin SHA; Event and Guide enter the roadmap as compiler/contract work (section G), not as adapter rows |
| R-06 | **Mobile performance and startup.** The bundle is 398 KB unminified and is parsed on every game page the loader matches; a 3-5x larger UI plus admin forms could double it | med/med | Measured in `A_ARCHITECTURE_MAP.md` §A.9: derived JSON is the largest cost; minification alone yields 233 KB; `boot()` returns early on unarmed pages but the parse cost is already paid | Slower page loads on every game page for the operator, not only in Forge; memory pressure on the phone increases tab eviction, the very thing the journal exists to survive | Size budget as a CI gate (section I); minified build with a reproducibility check; lazy contract data behind the arm marker; measure on the user's device in the first smoke of each phase |
| R-07 | **Repo sync failures.** Results, approval state or queue metadata written through the contents API race the `[auto]` workflows (`answers`, `skillpack`, `release_pin`, sentinel) that commit to `main` | med/med | `github.mjs:50` does a sha-aware PUT with no retry; automation uses `commit_generated.py` with rebase/retry (`docs/reviews/REPO_SIMPLIFICATION_AUDIT.md` F4); a bundle that fails to commit is shown as text for copy/paste (`app.mjs:402-405`); no `Results not synced` journal state exists today | An admin approval recorded locally but never landed in the repo; a results bundle lost after an eviction | Every repo write is journaled locally first with an "unsynced" state and a retry-on-demand action; new files go under paths no workflow rewrites; the design panel's queue model decides where approval state lives (user decision K-09). Owner copy and split state: CPY §14 `Results not synced` (CPY:441-453) and scenario S-39, live verified but repository not synced, never a generic failure (SCN:317-321) |
| R-08 | **Admin permission and publishing mistakes.** A content administrator publishes the wrong record, publishes with an incomplete dependency (hidden AI referenced by a visible quest), or a role change mid-session turns a green button into a server refusal | med/high | Publish semantics differ per class (hidden on jutsu/item/bloodline/quest/gameAsset; `published` on guides; no column on AI records; see `E_CONTENT_ADMIN_FEASIBILITY.md`); role gates are resolver-level (`permissions.ts canChangeContent`) and a refusal is an HTTP 200 `success:false` | The one action the brief wants to be "a few clear taps" is also the one with the highest blast radius | Publish is its own authority lane with its own confirmation, dependency check (referenced records' visibility read before the flip), read-back after the flip, and an audit entry; the admin surface exposes only what the signed-in role can do and re-checks before the send; user decisions K-03, K-05, K-10. Owner rules: SSC §11, publication must not visually mean execution succeeded (SSC:415); IRM §11 publish anti-patterns (IRM:333-341) |
| R-09 | **Migration of stored jobs, captures and settings.** Journal v1, IDB v2, `tnr_bk_*` keys and the PAT live on the user's phone; a schema change with a bad migration strands an open job with `SENT` items | low/high | `MIGRATIONS` is empty and `migrate()` refuses newer versions (`journal.mjs:404-423`); `repairHistory` already handles one class of corruption; retained keys are shared with Builder | A Forge Next release that cannot read an open 0.4.x job blocks reconciliation of a real write | Journal v2 only through an additive migration with fixtures from real exported journals; never delete a v1 record on failure (park under `<key>.corrupt` as `compat.mjs` already does); rollback = reinstall the previous pinned loader, so every migration must be readable by the previous version or explicitly refused |
| R-10 | **Builder retirement too early.** Retiring Builder before Forge covers the research-read, projection, zip-pack and batch workflows strands real work, with no escape path | med/high | Parity gaps are proven by committed manifests and bundles (`evidence/parity-matrix.json`, `evidence/harvest-evidence.json`); `push/05`/`push/06` ran on Builder v4.32 on 2026-09-12 | The user cannot capture PvP research or push a zip pack and has to reinstall Builder from history | Objective retirement gates (section J) each tied to a proven Builder use; Builder stays installed and pinned until every gate passes; a deprecation banner precedes removal; removal is a user ruling |
| R-11 | **Red baseline that CI does not see.** `npm test` at `main@305a28f` fails one release-loader test because the auto pin commit removes the `@x-release-pending` marker and `forge.yml` is not triggered by the bot commit | high/low | Local run in this session (292/293); Actions run 34545431895 succeeded at the merge commit `ce603def`; no run at `d1dbecc` | Every future branch starts red and the first "make CI green" commit is noise; a reviewer may distrust the suite | The consolidated ChatGPT branch already relaxes the assertion to "at most one marker" (`forge/test/release_loader.test.mjs` at `824c4d58`); once that slice integrates, phase 0 only has to confirm `main` is green and add the bot-commit trigger gap to the release checklist; the coupling of that fix to the feature branch is R-22 |
| R-12 | **Confirmation fatigue and `window.confirm`.** The current UI gates every consequential action with native dialogs; a bigger UI that keeps that pattern trains the operator to tap through | med/med | `app.mjs:172`; ten `app.confirm()` sites in `screens.mjs`; the Quest Studio slice reuses the same native confirm for draft-destroying actions (`824c4d58:forge/src/studio/ui.mjs:302,314`) | The publish confirmation becomes reflexive | Tiered confirmation model (section D): reads never confirm, mutations confirm once per job with the counts, publish requires an in-UI typed or held confirmation naming the record, deletions do not exist. Owner rules: IRM §5.5, confirmation fatigue is a safety defect (IRM:245-249); MOB §8, a structured custom confirmation with title, mode, consequence, scope, caveat, safe cancel and explicit verb (MOB:178-199) |
| R-13 | **Scope creep into a rewrite.** A UI replacement that touches runner/storage/transport "while we are here" invalidates 293 tests' worth of evidence | med/high | The layer boundary exists (`compose()`); domain logic already leaks into `ui/app.mjs` (§A.2) | The reviewed safety core is silently re-implemented | Phase 0 moves domain logic below the UI seam first; every later phase is UI-only or additive; a diff gate lists files under `runner/`, `storage/`, `transport/`, `budget/`, `reconcile/` touched per phase (section I) |
| R-14 | **Game-source dependency for admin features.** Some admin outcomes (native approval state, a dedicated publish toggle, role discovery without a probe) may need TheNinjaRPG changes, which are out of scope and outside the user's control | med/med | `E_CONTENT_ADMIN_FEASIBILITY.md` marks per-class "would need game change" | A phase is planned around a capability the game does not offer | Each admin capability is classified zero-change / partial / needs-game-change with citations; needs-game-change items are dependencies recorded in K, never phase deliverables |
| R-15 | **Two loaders on one origin.** Builder (`document-idle`) and Forge (`document-start`) both match the whole origin; Forge removes Builder's root nodes while mounted (`takeover.mjs:53,171`) | low/med | Source-verified; unverified in a real browser for the new overlay sizes | Builder's panel reappears over the admin surface, or Forge's removal observer eats a future game node with a colliding class name | Keep the suppression scoped to the two known classes; retirement plan makes Builder read-only before removal (section J) |
| R-16 | **Mockup-implied capabilities without a source-verified path.** The approved concept shows actions and lanes that do not correspond to a verified procedure or to how content is produced today ("Create Content", "Edit & Write", "Create Manifest / package for deployment", "Validate Content: rules, balance, format", "Game Source Connection: Online", lanes "Locations", "Characters", "Systems") | high/med | Amendment §12 "Must not be inferred from the concept"; manifests are authored in the repository by the content agent and run by the operator (`docs/DOCTRINE.md` D-user-pushes); balance is user-owned (D-reserved-dauntless); a status probe that polls the game spends budget (`R-03`) | A phase is planned around a lane or button with nothing verified behind it; or a status light implies a live check that was never made | Every mockup element is classified keep / rename / regroup / drop / needs-capability-first in `D_IA_AND_JOURNEYS.md` §D.2 with the capability row that justifies it; "status" blocks show only state Forge already holds (auth probe result, cached listing age, budget, journal), never a fresh poll; departures from the approved character go to the director (§D.7). Owner rules: WCM §23, components that must not imply more than source and permissions prove (WCM:611-629); the board's mode selector must not make authority look like a preference (SBI:282,431) |
| R-17 | **Studio seam operating costs.** The Quest Studio foundation dispatches a GitHub Actions worker per compile, creates one `studio/quest/<id>` branch per request with no cleanup, polls the Contents API every 2 s for up to 60 s, and needs a PAT with Actions dispatch and branch-creation rights on the operator's phone | med/med | `A_ARCHITECTURE_MAP.md` §A.11 (`repository.mjs submit()/dispatch()/buildResult()`, `quest_studio.yml`). Added from the consolidated reading: `scrub.yml:2` runs on every push with no branch filter, so each PAT branch creation and each source save on `studio/quest/<id>` starts a scrub run as well (Inferred; the trigger rule is GitHub's, not cited in-repo); `824c4d58:.github/workflows/quest_studio.yml:26-28` sets a per-request concurrency group with `cancel-in-progress: true`, so two Compiles racing on one requestId cancel the in-flight build and its result never lands, while the UI only discards the older poll (`824c4d58:forge/src/studio/ui.mjs:412-419`) and nothing tells the operator; validation refusals exit 2/3 before the persist step (`824c4d58:.github/workflows/quest_studio.yml:59-77`) and a missing compiler result exits 4 (`:106`), so no result lands and `buildResult()` returns null on 404 (`824c4d58:forge/src/studio/repository.mjs:115-117`), leaving the UI in `Build still running` after 30 polls (`824c4d58:forge/src/studio/ui.mjs:385,446-449`), indistinguishable from a slow queue | Branch sprawl and API quota exhaustion on a busy authoring day; a broader PAT on a mobile browser; a compile that silently never lands if the worker queue is slow, was refused, or was cancelled by a second tap (the UI shows "still running" in all three cases) | Roadmap phase for the Studio adds branch lifecycle (archive on package gate, delete on abandon; K-37), a worker refusal envelope and cancellation visibility (K-38), exponential poll backoff with a visible "check later" state, and a least-privilege PAT policy under K-15/K-26; the trigger model (dispatch vs source-push) is an open decision precisely because it changes the PAT scope (CF-16); the seam's own colour and shell debt is carried by R-24 and R-28 |
| R-18 | **Studio state outside the journal.** Drafts and build results live in one `localStorage` key and in-memory `buildState`; a second draft, a tab eviction mid-compile or a Studio-branch manifest entering the runner have no journaled state today | med/med | `forge/src/studio/ui.mjs:10,146-158,409-450` at `824c4d58`; the code itself says a promotion/resume contract is still needed. Concrete facts from the consolidated reading: one draft per device, the draft and its `lastResult` share one key (`824c4d58:forge/src/studio/ui.mjs:10,148,157`) and `buildState` is memory only; the requestId is `quest-` plus base36 of the device clock (`824c4d58:forge/src/studio/ui.mjs:62-64`), so two devices cannot share a draft and one device keeps only its latest; `New Mission` replaces the local draft after a native confirm (`:306-316`); re-compiling the same id overwrites the source file through a sha-aware branch PUT (`824c4d58:forge/src/github.mjs:111-128`) and the worker `rm -rf`s the previous result and build directory (`824c4d58:.github/workflows/quest_studio.yml:120`); `pollBuild` returns silently when the shell is hidden (`824c4d58:forge/src/studio/ui.mjs:435`); no journal entry exists for any of it; the only route from a valid build to the runner is `showExport` and a manual commit to `push/` (`824c4d58:forge/src/studio/ui.mjs:469-474`; picker at `app.mjs:186-207`) | A valid build cannot be run without a copy/paste step (exactly the relay the ruling wants removed), or a rushed promotion path bypasses `parseManifest`/`planOrder` and the auth gate; a phone that backgrounds the tab mid-compile comes back to a stale "still running" | Section F makes "Studio package → runner" a journaled promotion with the same preflight as `push/` manifests (K-39); drafts become durable per-request records with repository-sync state (`Draft saved locally` / `Repository revision saved` / `Build requested` / `Building` / `Build result received`, per RB §11, RB:271-276) |
| R-19 | **Forge bundles break the repository's own parity guard.** A Forge results bundle carries `checks: null`; `validate.py --parity` does `set(bundle.get("checks", []))` and raises `TypeError`; `session_close.py --guards` runs parity on the lexically newest inbox bundle, so the session-close guard is red whenever a Forge bundle is newest | high/med | Behaviour-proven: reproduced in this pass on `harvests/inbox/tnr_results_1789124514980.json` (Forge 0.4.0); a Builder bundle passes (`evidence/repo-consumers.json`, bundle-contract rows) | Every Forge run makes the content session's close ritual fail until a Builder bundle lands, which is a hidden pressure to keep Builder alive | Phase 0: either emit a real `checks` inventory from Forge's own validator/lints (the honest option) or make parity skip `cfg:"forge"` bundles explicitly; `forge/test/harvest.test.mjs` grows a parity case; this is a Builder retirement gate (section J) |
| R-20 | **Browser-vs-canonical rule drift.** The Studio's browser-side draft check encodes mission-shape policy that the canonical compiler does not enforce, so the two sides can disagree and each rule change needs a second rewrite in the UI | med/med (est.) | Source-verified: `missionDraftProblems()` refuses profiles with `shape.battle_nodes > 0` and requires `beats + 1 == shape.objective_count` (`824c4d58:forge/src/studio/ui.mjs:122-138`); `mission.py`'s only shape check is the per-node headcount ceiling (`skills/building-tnr-content/scripts/mission.py:156-174`) and `objective_count` appears there only in the selftest fixture (`mission.py:351`); the registry and profiles are fetched from `main` when the Studio opens (`824c4d58:forge/src/studio/repository.mjs:57-66`) while the compiler reads `main` at dispatch, so the two can also see different revisions. RB §4 names exactly this "bad pattern" (RB:95-97) and RB §13's second acceptance test is not met for the shape rules (RB:309-311); FBRF §10 asks reviewers whether browser rules duplicate canonical policy (FBRF:129) | A draft is refused locally that the repository would accept, or a source the repository accepts violates the profile shape; improving `mission.py` or a profile does not improve Forge until someone re-types the rule in JavaScript | Gate: generated-projection test. Browser shape rules are derived from a repository-generated contract (section F, registry-driven UI) and a test fails when `ui.mjs` encodes a rule the generated source does not carry; until that lands, the browser rule is labelled advisory in the UI and the compiler result stays the authority (RUL-2026-09-12-002) |
| R-21 | **Prose-coupled blocker classification.** `blocked` (a director decision is missing) versus `failed` (a tool defect) is decided by matching literal substrings of `MissionError` messages | med/high (est.) | Source-verified: `classify_mission_error()` recognises only three literal markers (`824c4d58:skills/building-tnr-content/scripts/quest_compile.py:108-115`); `MissionError` carries no structured code (`skills/building-tnr-content/scripts/mission.py:64`); the matched strings live in prose at `mission.py:132` ("balance call") and `mission.py:192` ("rulings, not defaults"); FBRF §10 makes preserving that difference an explicit review focus (FBRF:130) | A wording change in `mission.py` silently turns a `decision_required` blocker into a red "Build failed"; the operator files a tool bug instead of asking the director, or a decision card never appears | Gate: code-based classification test. `mission.py` emits a structured error code (a Lane A brief on the canonical owner, never a UI-side fix, CLAUDE.md §7), `quest_compile.py` classifies on the code, and a test fails when a classification depends on message text; until then the three markers are covered by a selftest that breaks when either side changes them |
| R-22 | **CI duplication, and the red-baseline fix coupled to the feature branch.** The Studio slice adds a second Node CI workflow and carries the only change that makes `main` green together with unrelated loader-assertion rewrites | high/low (est.) | Source-verified: `824c4d58:.github/workflows/quest_studio_ci.yml:3-28` triggers a second `npm ci`/`npm test`/build job on Studio paths beside `forge.yml`, and it has no `npm audit` step (`forge.yml:44` has one); the marker relaxation that fixes R-11 (`824c4d58:forge/test/release_loader.test.mjs:47-51`) sits in the same file and branch as the rewritten `@match`, unarmed-page and entry-point assertions (`824c4d58:forge/test/release_loader.test.mjs:86-102`), which drop the `/forge`-only negative check `main` has (`forge/test/release_loader.test.mjs:96`; diff read in this pass) | Two CI runs per Studio push with different gate sets; phase 0 cannot turn `main` green without integrating or cherry-picking a feature branch it may not touch; the same test edited on two branches breaks the one-writer rule (CF-19) | Gate: phase-0 CI consolidation. One Node workflow with one gate set (audit, test, fixtures, bundle diff, size budget); the marker-only fix is either integrated with the slice or separated as a minimal assignment, as the director assigns (CF-19, RM-05); Fable authors no competing `release_loader` change without that assignment |
| R-23 | **Canonical under-enforcement of the mission profile shape.** "The profile owns the shape" is doctrine in `48_DATA` and the quest reference, but `mission.py` does not enforce `objective_count` or `battle_nodes`; the Studio exposes the gap because the browser enforces them and the compiler does not | med/med (est.) | Source-verified: `D_combat` declares `objective_count: 4, battle_nodes: 1` (`skills/building-tnr-content/data/48_DATA_mission_profiles.json:187-188`); `mission.py:156-174` enforces only `max_enemies_per_battle`; the consolidated reading compiled a `D_combat` source with zero battles as valid in an overlay (Observed, foundation-impl G7); FBRF §4 keeps mission policy with `48_DATA` plus `mission.py` (FBRF:53) | A Studio that trusts the compiler ships a mission whose shape contradicts its profile; a Studio that trusts its own rule diverges from canon (R-20); fixing it in the UI is the rule proliferation CLAUDE.md §7 forbids | Gate: K-36 ruling first (enforce canonically, with or without a strict flag, or declare the shape advisory), then a `mission.py --selftest` case on the owner under its own Lane A brief (RM-09); the browser rule becomes a projection either way |
| R-24 | **Copy or visual grammar collapses axes.** A palette, label set or component that merges operation context, job state, outcome, auth, budget and content lifecycle lets one cue mean two things | med/high (est.) | Owner contracts: SSC §2 requires at least six semantically separate axes (SSC:22-30); COL §4 collision rules (COL:41-49); ICN §19 lists the combinations that must stay distinguishable (ICN:386-400). Today: a `DONE` pill is styled as success regardless of outcome (`forge/src/ui/styles.mjs:44`; CF-02); the Studio compile control is `#f97316` (`824c4d58:forge/src/studio/ui.mjs:39`), the same hex the style board assigns to Publish (SBI:113), so a compile tap already borrows publish identity; Studio build states (`824c4d58:forge/src/studio/ui.mjs:380-406`) belong on the content-lifecycle axis F (SSC:99-105), not the execution axis (RM-06) | "Mechanically valid" reads as a live success or a publication; a warning inside live-write mode reads as the mode itself; a crimson lane reads as failure | Gate: scenario snapshot suite S-64..S-67 (SCN:433-447) plus every ICN §19 combination, rendered from fixtures and kept green per phase; the Studio control takes a construction from the design-system token set when the shell is absorbed (CF-22) |
| R-25 | **Toast-only critical state.** A transient notification is the only surface for a state that must persist until resolved | med/high (est.) | Owner contracts: WFA §16 lists the states a toast may never carry alone (WFA:484-505); SCR §16 requires those states on a durable surface until resolved or safely dismissed (SCR:450-464). Today the `SESSION` pause is announced by a 12 s toast (`forge/src/ui/app.mjs:293`) beside a banner; the banner, not the toast, is the durable surface | A bigger UI with more transient feedback lets a `SENT` ambiguity, an auth refusal or an unverified finish scroll away while the operator is on another screen | Gate: durable-surface contract test. Every state on the WFA §16 bad-toast list has a persistent in-workflow surface asserted by a UI-contract test; toasts are limited to the WFA §16 good cases |
| R-26 | **Authorization denial rendered without a signal.** Transport cannot tell a role denial from any other HTTP-200 refusal, so a "Permission denied" label would be invented | med/med (est.) | Source-verified: an HTTP-200 `success:false` becomes `kind: "refused"` (`forge/src/transport/outcome.mjs:39`) and the item is `FAILED` with `refused: <message>` (`forge/src/runner/runner.mjs:640`); no FORBIDDEN class exists. Owner contracts: CPY §10 allows `Permission denied` only when the server or source proves role denial (CPY:384-397); SSC §6 keeps auth state global, never disguised as a job result (SSC:264). Section E has now audited the pin: role denial is per procedure, not per session; the refusal carries no code field, so the only candidate signal is the message string; it is stable for thirteen named paths and indistinguishable for four that guard with `entry && canChangeContent` (`E_CONTENT_ADMIN_FEASIBILITY.md` E.3) | A redesign labels every refusal "Permission denied" (wrong for validation refusals) or hides a real role refusal as a generic failure and tells the user to sign in again | Gate: render `Permission denied` only when section E cites a stable source signal (K-34 audit, CF-04); otherwise render `Write refused` with the server message under disclosure; a test asserts the label is conditional on the classifier, never on copy. Resolved construction: no fifth session state; authorization is handled pre-emptively from a role pre-check (disable what the role cannot do, re-check on send), and the string-matched per-action label ships only if K-34 admits it, as a generated mapping with a test that fails when an upstream message changes (the R-21 discipline) |
| R-27 | **A redesign drops one of the thirteen UI-level semantic guarantees.** Labels and layout may change, but the thirteen guarantees AUD §17 lists must survive with the same meaning | med/high (est.) | AUD:457-469 lists them (Tier D before-state used here as the acceptance list); each maps to code today (BF-54), for example read-only vs mutation (`forge/src/ui/screens.mjs:120-122`), `SENT` reconciled not retried (`forge/src/runner/runner.mjs:193`), `CONFIRMED` not `VERIFIED` (`forge/src/ui/styles.mjs:43-44`), no carrier contamination (`forge/src/ui/styles.mjs:1-9`, `forge/src/ui/takeover.mjs:177-182`); existing UI-contract tests cover five of them (`forge/test/ui.test.mjs:96,135,162,206,254`). R-01, R-12 and R-13 each cover only part of the list | A label or layout change quietly removes a guarantee that no test names; the review cannot see it because the before-state list lives in a design document, not in the suite | Gate: section I maps each of the thirteen guarantees to a kept-green test, with SSC as the meaning owner; a phase cannot close with an unmapped guarantee |
| R-28 | **A new shell or theme contaminates, or fails to release, the carrier page.** Forge mounts inside a live game route; a global reset, `:root` tokens or an unreleased overlay restyles or blocks the game | low/high (est.) | Source-verified: the scoping rule is stated as a correctness rule (`forge/src/ui/styles.mjs:1-9`); tokens live on `.f-host`/`.f-app`, not `:root` (`forge/src/ui/styles.mjs:19-21`); `CSS_DOC` is installed only on the entry splash (`forge/src/main.mjs:91`); the adopted stylesheet outlives the overlay node (`forge/src/ui/styles.mjs:6-7`); `release()` disconnects the observer, removes the host and restores overflow (`forge/src/ui/takeover.mjs:177-182`). The Studio slice ships a second fixed overlay with its own adopted stylesheet (`824c4d58:forge/src/studio/ui.mjs:12-60`, installed on the document at `:177`), installed after `compose()` returns (`824c4d58:forge/src/main.mjs:151`); its shell is appended to the app root (`824c4d58:forge/src/studio/ui.mjs:189`) and Close only hides it (`:201-203`), so the second surface has no release path of its own and its stylesheet outlives the overlay exactly as the first one does (BF-09) | The game page stays restyled after Close; a second surface's stylesheet is left adopted on the document; a theme token set declared on `:root` leaks into the carrier | Gate: stylesheet allow-list and contrast contract test (`D_VISUAL_SYSTEM.md:653-655`: every selector scoped under `.f-host`/`.f-app`/`.f-boot`, animation only on allow-listed selectors) plus a release test that asserts no Forge node or style remains after `release()`; one shell after the Studio absorption (CF-22) |
| R-29 | **A template library canonises the flatten experiment.** Studio templates or defaults turn the staged four-node Forsworn rewrite into the universal mission shape before the deferred ruling | med/med (est.) | MS §2: the Studio must not make `dialog → start_battle → dialog → win_quest` the template, rewrite profile-shape policy to match it, label longer shapes invalid or treat `start_battle` as mandatory (MS:40-58, Tier B); the mission-shape doctrine rewrite is explicitly deferred pending player feedback and may not be folded into the law-reconciliation pass (`docs/RULINGS.md:84` at `824c4d58`) | A "new mission" default or a starred template silently sets global policy; a later ruling the other way strands every draft built from it | Gate: Studio defaults come from the selected `48_DATA` profile and the approved project brief only; no template asserts a shape its profile does not declare; a review checkbox in every Studio phase until the deferred ruling lands (section K carries it as open) |
| R-30 | **Accepted art without repository bytes blocks packaging.** Acceptance recorded in chat or in a ledger, with the bytes outside the repository, leaves a package that cannot be built | med/med (est.) | Live example: `art.intake_accepted_assets` is BLOCKED because the accepted bytes are not present anywhere in the repository at the verified `main` SHA (`state/workstreams/one_perfect_crop/ROADMAP.md:39,65-68`); the boundary is `docs/workflows/ART_PRODUCTION.md:132-136` (art production does not write the live game; an asset is a production input, not authorization to publish) under `docs/DOCTRINE.md:75` D-user-pushes; the "standing ruling" the Expansion cites has no RUL id (CF-17) | A Studio "accept art" affordance records acceptance without bytes; the package gate then waits on a re-supply the operator thought was done; or acceptance is read as permission to publish | Gate: the packaging gate requires repository bytes for every accepted asset, and acceptance state is a projection of a durable artifact, never a free-standing flag; whether to record the boundary as a RUL entry is the director's (K-41, CF-17); the infographic lane stays outside the game-art pipeline by design (EXP:170-172,505; K-29) |

| R-31 | **`hidden` is a listing and play gate, not confidentiality.** A review or staging model that treats `hidden:true` as "not visible to players yet" in the secrecy sense is wrong: any caller can read an unreleased record by id, and any caller can list unreleased records by asking for them | med/med (est.) | Verified in this pass at the Forge pin `345d18ac`: `quests.get` is a `publicProcedure` that returns the record whatever its `hidden` value, with the only refusal being NPC-only quest types (`app/src/server/api/routers/quests.ts:215-236`); `jutsu.getAll` filters on the caller's own `hidden` input and only defaults to `hidden:false` (`app/src/server/api/routers/jutsu.ts:2115-2117`); the deep dive found the same shape across the content classes and records it as cross-cutting row CC-08 (`evidence/admin-feasibility.json`, forthcoming in `E_CONTENT_ADMIN_FEASIBILITY.md`) | A Content Admin surface that promises "draft" or "not yet public" in the confidentiality sense over-claims what the game enforces; a publish flow that treats the flip as "first exposure" is telling the operator something the source does not support. It also means an embargo cannot be enforced by Forge at all | Section E states the per-class visibility semantics in the operator's words, and section D's publish copy says what the flip actually changes (listing and play eligibility, not readability). No Forge screen calls a hidden record confidential. Whether an embargo requirement exists at all is a director question routed to K-07 |

## H.1 Risks accepted for the planning pass itself

- No browser or live-session behaviour was measured; every claim about Firefox Android, ViolentMonkey timing, `navigator.storage.persist()`, Clerk refresh mid-job, real 429 clocks or overlay survival across navigation is Inferred and is listed in the handoff for the user's smoke tests.
- The game head used for drift is a point in time (`36c5873b`, 2026-09-12); the roadmap's first implementation brief must re-run the drift tools against the head of that day.
- Transcript reconciliation had not landed at `824c4d58` (HND:3 still reads pending; HI-14), so the operation-mode taxonomy and mode-colour question is a four-way disagreement between VD §8, COL §2/§5, DS v0.1 §4 and the style board (CF-06). It is held open under K-21/K-22/K-33 (pending predecessor-transcript reconciliation) and nothing in this register treats any of the four as settled.
- The Studio seam measurements behind R-17, R-18 and R-20..R-23 were taken in read-only overlays of the exported branch files (foundation-impl notes), not on the branch's committed `forge_bundle.js`; the brief's "fresh build equals checked bundle" gate is unverified here and is review focus for the seam's owner (RM-02).
- GitHub's permission classes for workflow dispatch and ref creation, the trigger rule behind `scrub.yml` firing on PAT pushes to `studio/quest/*`, and the rule that `GITHUB_TOKEN` pushes do not start further runs are external platform facts and are marked Inferred wherever R-17 and K-26 rely on them.
