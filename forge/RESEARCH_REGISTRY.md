<!-- GENERATED by forge/tools/derive_registry.mjs from forge/src/research/registry.mjs. Do not edit by hand. -->
# Forge research-read registry

Every read Forge will issue as a research capture, and nothing else. A path without a row here is
refused before transport, whatever the game would answer (`RUL-2026-09-17-002`).

- **Source pin audited:** `345d18accf6d8ea8d8d47ef0e61b5aff7d5a1cf9` (`studie-tech/TheNinjaRPG`), the same pin
  `forge/src/runner/fields.json` and `nested.json` carry; `tools/check_boundaries.mjs` holds the three equal.
- **Tier** is both the default and the ceiling. A manifest may ask for that tier or any narrower one
  (`local-only` < `projected` < `repo-safe`); widening one is a reviewed edit of the registry module,
  never a manifest key (`RUL-2026-09-17-001`).
- **Kind, auth and limiter** are not restated here. They are read from
  `forge/src/transport/procedures.mjs`, which owns the source facts; this table only shows them.
- **Paged** reads are bounded twice: by the row's own audited maximum limit, and by the
  `MAX_PAGES` = 20 ceiling no manifest can exceed.

| path | kind | auth | limited | read mode | input contract | paging | tier | demanded by | audited at |
|---|---|---|---|---|---|---|---|---|---|
| `gameAsset.get` | query | public | yes | point | `id` | — | `repo-safe` | push/02_one_perfect_crop_asset_probe.json | routers/gameAsset.ts get |
| `jutsu.get` | query | public | yes | point | `id` | — | `repo-safe` | push/05_aerathiel_pvp_research_stage1.json | routers/jutsu.ts get |
| `item.get` | query | public | yes | point | `id` | — | `repo-safe` | push/24_godstorm_tower_related_capture.json | routers/item.ts get |
| `bloodline.get` | query | public | yes | point | `id` | — | `repo-safe` | push/21_night_parade_asset_followup.json | routers/bloodline.ts get |
| `quests.get` | query | public | yes | point | `id` | — | `repo-safe` | push/01_old_ghost_format_capture.json | routers/quests.ts get |
| `profile.getAi` | query | protected | no | point | `userId` | — | `repo-safe` | push/04_one_perfect_crop_bandit_ai_probe.json | routers/profile.ts:1121 getAi |
| `ai.getAiProfile` | query | protected | no | point | `id` | — | `repo-safe` | push/25_godstorm_tower_combat_closure.json | routers/ai.ts getAiProfile |
| `jutsu.getAllNames` | query | public | yes | list | _(none)_ | — | `local-only` | push/00_forge_readonly_smoke.json | routers/jutsu.ts getAllNames |
| `quests.getAllNames` | query | public | yes | list | _(none)_ | — | `local-only` | push/00_forge_readonly_smoke.json | routers/quests.ts getAllNames |
| `item.getAllNames` | query | public | yes | list | _(none)_ | — | `local-only` | builder dedupNames live-name check | routers/item.ts getAllNames |
| `bloodline.getAllNames` | query | public | yes | list | _(none)_ | — | `local-only` | builder dedupNames live-name check | routers/bloodline.ts getAllNames |
| `gameAsset.getAllNames` | query | public | yes | list | _(none)_ | — | `local-only` | builder dedupNames live-name check | routers/gameAsset.ts getAllNames |
| `profile.getAllAiNames` | query | public | yes | list | _(none)_ | — | `local-only` | builder dedupNames live-name check | routers/profile.ts getAllAiNames |
| `combat.getBattleHistory` | query | protected | no | query | `userId?` `secondsBack?` `combatTypes?` | — | `local-only` | push/05_aerathiel_pvp_research_stage1.json | app/src/server/api/routers/combat.ts:530-560 getBattleHistory |
| `combat.getBattleEntries` | query | protected | no | query | `battleId` `limit?` `offset?` `userFilter?` `showBasicActions?` `refreshKey?` `checkBattle?` | offset, default limit 30, max 500 | `local-only` | push/06_aerathiel_pvp_battle_logs.json | app/src/server/api/routers/combat.ts:382-421 getBattleEntries |

## Adding a row

1. a committed, reviewed manifest or work package must actually need the procedure;
2. audit its contract at the declared pin — kind, auth, limiter, exact input, pagination — and record
   the file and line;
3. add the row at tier `local-only` unless it is a content-record point read already approved for
   repository persistence;
4. add the evidence to `forge/test/research.registry.test.mjs`;
5. regenerate this file with `node tools/derive_registry.mjs`.
